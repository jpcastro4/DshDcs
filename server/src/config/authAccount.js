const Account = require('../api/account/account')

module.exports = (req, res, next )=>{

    console.log(req)

    // const authAccountURL = (req, res, next) => {
    //     Account.findOne({ req.})
    // }
}